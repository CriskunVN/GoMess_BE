{
  "watch": ["src"],
  "ext": "ts,js,json",
  "ignore": ["src/**/*.test.ts", "dist/**"],
  "exec": "npm run build && node dist/server.js",
  "delay": 1000,
  "env": {
    "NODE_ENV": "development"
  },
  "verbose": true
}
